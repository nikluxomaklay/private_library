SECRET_KEY:

DEBUG: False

ALLOWED_HOSTS:
  - 'localhost'
  - '127.0.0.1'

DATABASES:
  ENGINE: 'django.db.backends.postgresql'
  PARAMS:
    OPTIONS:
      service: '.default.pg_service'
      passfile: '.default.pgpass'

STATIC_ROOT: